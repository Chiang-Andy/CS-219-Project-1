#ifndef ANALYZER_H
#define ANALYZER_H

#include <iostream>
#include <fstream>
#include <string>
#include <sstream>

using namespace std;

uint32_t getHex(uint32_t);
void hexConversion(uint32_t);
uint32_t hexAddition(uint32_t, uint32_t);

#endif